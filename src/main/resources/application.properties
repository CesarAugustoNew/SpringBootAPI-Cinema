spring.application.name=Filmes

# Servidor
server.port=8080

# Banco de dados MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/cinema?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Cesar123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Swagger
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# JWT
jwt.secret=cinema-reserva-jwt-chave-secreta-muito-segura-256-bits-ou-mais-longa-aqui
jwt.expiration=86400000

# Upload de imagens (pôsteres dos filmes)
# Pasta onde os arquivos ficam salvos; servida publicamente em /uploads/**
app.upload.dir=uploads